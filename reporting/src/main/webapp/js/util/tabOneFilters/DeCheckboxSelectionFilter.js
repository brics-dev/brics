/**
 * 
 */
QT.Filters.DeCheckboxSelectionFilter = QT.Filter.extend({
	name : "DeCheckboxSelectionFilter"
	// the filter here is treated just like a text filter so no extra processing needed
});